# Pin CPU-only PyTorch — prevents unstructured/unstructured-inference from
# pulling the full CUDA variant (~4 GB nvidia-* packages).
# Must use --extra-index-url https://download.pytorch.org/whl/cpu when installing.
torch==2.11.0+cpu
